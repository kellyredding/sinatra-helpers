require 'rubygems'
require 'bundler'
Bundler.setup

require 'sinatra'
Bundler.require(:default)

require 'app/base'

# define routes here or load in route definitions

get '/' do
  erb :'index.html'
end
